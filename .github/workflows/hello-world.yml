name: Print environment variable
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Print environment variable
      run: echo "The value of REPOS_VISIBLE_TO_ALL_VAR is $REPOS_VISIBLE_TO_ALL_VAR"